# All of these were already present on the development machine; the file exists
# so a fresh clone does not have to discover them by crashing.
httpx>=0.27
beautifulsoup4>=4.12
lxml>=5.0
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0
