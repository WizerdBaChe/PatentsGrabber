# Stage 1 深度回顧 — 追溯矩陣與儀器體檢

- 日期：2026-08-27 · 對象：`main` @ `0cc20b1`（Stage 1 全部併入後）
- **狀態：§4 的四條建議已於同日全部落地並實跑通過**；§2／§3.2／§3.3 的相關列已就地更新，
  更新處都標了日期。沒有改動任何既有行為。
- 這份文件不新增需求、不改任何行為。它回答兩個問題：
  **(A) 每一條規則／需求，今天由誰守著？** **(B) 那些守門員本身可信嗎？**
- 讀法：表格裡的「證據等級」是全文的重點欄位。
  - **可重跑** = 有一支現在就能執行的檢查會失敗
  - **一次性** = 當時量過、寫進紀錄，但沒有東西會在它壞掉時叫
  - **無** = 只有程式碼本身
- 產出的建議集中在 §4，且刻意很短。不是每個「無」都值得補。

---

## 0. 方法與可翻盤條件

**怎麼查的**：對 `tools/*.py`、`tests/`、`src/patentsgrabber/` 逐一比對 CIM §6 的 BR-1…BR-8、
`docs/05` §4 的 R-1…R-10、§2 的 F-1…F-20，記錄每條規則的實作位置與斷言位置。
所有 gate 於同日在乾淨樹上重跑過（結果見 `docs/05` §5.5）。

**這份回顧的成立條件**：它描述的是 `0cc20b1` 這一份程式碼。任何一次改動 gate 或
擷取器都會使 §1–§2 的表格部分失效——**推翻方式**：重跑一次同樣的比對，成本約 20 分鐘。

**一個要先更正的印象**：上一輪對話裡我說「`R-*` 在 gate 與原始碼中一次都沒出現」——
字面為真（那些 ID 確實沒被寫進程式），但由此推論「閱讀需求全靠人眼」是過頭了。
逐條比對後，**R-1 / R-4 / R-5 / R-6 其實有機器守門**，只是沒掛 ID。真正沒人守的是
瀏覽器那一層（R-2 / R-7 / R-8 / R-9 / R-10）。下面的表格是更正後的版本。

---

## 1. 業務規則 BR-1…BR-8

| ID | 實作位置 | 誰在守 | 證據等級 |
|---|---|---|---|
| **BR-1** 輸入寬容 | `numbers.py`、`service.lookup` 分派 | `check_search.py` §6（號碼／公司名／`in=` 前綴／台積電，misrouted 0）、§7（EP 號碼）；`tests/test_ops_number_formats.py` 7 案；`smoke_service.py`（裸號碼、申請號、亂字串） | **可重跑**，本專案覆蓋最完整的一條 |
| **BR-2** 來源依欄位而變 | `service.py` 建卡路徑（文字←Google／圖式←OPS） | 無直接斷言。`verify_ops.py` §4 只確認「OPS 對 US 沒有全文」，那是 **來源事實**，不是「卡片確實照這個分工取值」 | **無**（間接） |
| **BR-3** 缺漏必須自曝 | 各失敗路徑的 reason 字串 → UI | `smoke_service.py` 3 案（申請號／不存在號／亂字串）、`check_search.py` §5（零結果附欄位說明） | **可重跑（僅輸入端）**；OPS 失敗端（404／403／無憑證降級）是 **一次性**（`docs/05` §5.1） |
| **BR-4** 每個欄位標來源 | `provenance[field]` | `smoke_service.py` 1 案：claims 的 provenance 選擇器（Google 那半） | **一次性**（OPS 來源那半只在 §5.1／§5.3 的瀏覽器檢查中看過） |
| **BR-5** 查過即入庫 | `store.py`、`var/library.sqlite3`、`var/ops-cache/` | `smoke_service.py`：第二次查詢由本地庫服務、庫內筆數 | **可重跑（資料庫半邊）**；`ops-cache` 半邊是 **一次性**（PDF 11.4 s → 0.00 s） |
| **BR-6** 配額自我節制 | `epo_ops.Usage`、隨看隨取的頁面位元組 | `verify_ops.py` §7 與 `check_search.py` §8 **只印不斷言**；「建卡不預抓頁面位元組」無檢查 | **無**（只有可讀的計數） |
| **BR-7** 只碰單件頁 | `google_patents.BASE = ".../patent/{number}/{lang}"` | 無測試。但這是 **結構性保證**：整個模組只有這一個 URL 模板，沒有可組出搜尋端點的路徑 | **無斷言，結構安全** |
| **BR-8** 名稱變體攤開 | 檢索結果的 applicant 聚合 | `check_search.py` §4 四項（多拼法、as-filed 保留、收斂路徑、`&` 進 CQL）＋ `verify_ops.py` §6 | **可重跑** |

---

## 2. 閱讀需求 R-1…R-10

| ID | 誰在守 | 證據等級 |
|---|---|---|
| **R-1** 描述分塊、段號在側欄 | `check_reading.py`：`blocks == 0` 即失敗、空塊即失敗、覆蓋率須落在 [0.98, 1.02] | **可重跑**（20 份文件） |
| **R-2** 排版可調且持久化 | 無。`localStorage` 的寫入與回讀沒有任何自動檢查 | **一次性** |
| **R-3** 章節大綱與跳轉 | 半條：`check_reading.py` **計數** headings 但 **不斷言**（`analyse()` 有 `headings` 欄，`verdict()` 不看它）。跳轉行為無檢查 | **無斷言（有可讀數字）** |
| **R-4** `FIG. n` 可點 | `check_reading.py` 正反對照：三份應有 figref 的文件 > 0、三份不應有的 = 0。點擊行為本身無檢查 | **可重跑（抽取半邊）** |
| **R-5** 請求項樹 | `check_reading.py` 的 claim coverage，且該指標對「舊解析器」重播過（0.282 會被擋下） | **可重跑，且已校準** |
| **R-6** 純文字可複製 | 覆蓋率指標正是這條：結構化後的字數必須貼齊原始扁平文字 | **可重跑** |
| **R-7** 元件編號高亮 | 無。UI 端 `#c-numeral`、`READ_DEFAULTS.numerals=false`；抽取端 `figure-callout` | **無** |
| **R-8** 不得靜默等待 | 無。§5.2 記錄了觀察到的階段字串與計時器 | **一次性** |
| **R-9** 版面隨視窗 | `check_layout.py` §1–§2：1920／2560 兩個尺寸下「面板減欄寬 ≤ 220 px」與「無水平溢位」，另有兩個校準案 | **可重跑，已校準**（2026-08-27 補） |
| **R-10** 字級與行寬隨螢幕 | `check_layout.py` §4–§5：自動配對隨視窗變大且落在 16–18px／68–76ch，且**讀者自訂值不被覆寫**（雙向） | **可重跑，雙向對照**（2026-08-27 補） |

**結論**：機器守得住的是「**文字有沒有被弄丟、結構有沒有被抽出來**」；守不住的是「**讀起來對不對**」。
這個分界其實合理——後者本來就需要人眼——但 R-9／R-10 是例外：它們是**可量測的數字**
（欄寬、面板寬、有無水平溢位），原本卻被當成觀感在處理。**這個例外已於 2026-08-27 補上**
（`tools/check_layout.py`，見 §4 建議 3）；R-2／R-7／R-8 仍然是人眼的事，且刻意不補。

---

## 3. 儀器體檢：守門員本身可信嗎

### 3.1 做得對的地方（要保留，不要在重構時弄掉）

| 儀器紀律 | 在哪 |
|---|---|
| **壞輸入必須被擋下**（負向校準） | `check_reading.py`：拆掉段落標記的頁面必須失敗，且會明說「checker 沒在量任何東西」 |
| **指標對它要抓的缺陷校準過** | claim coverage 對舊解析器重播 → 0.282 擋下、1.000 放行；巢狀樣本在新舊兩版都 1.000（不會亂叫） |
| **查詢建構的校準** | `check_search.py` §1：手工兩子句查詢必須被 matcher 拒絕，證明這個檢查有能力失敗 |
| **系統性故障偵測** | `check_reading.py`：某個指標在**所有**文件上都是 0 → 印 `SYSTEMATIC FAILURE`，把「全體一致」視為儀器故障而非結論 |
| **正反雙向對照** | figref 三正三負；EN/DE/FR 附屬項九案雙向；零結果是答案不是錯誤 |
| **紅燈轉為斷言而非刪除** | `verify_ops.py` §4：US 全文不可用從「失敗」改寫成「預期不可用」，OPS 哪天開放反而會 FAIL——這是**設計成警報的紅燈** |

這一組是 Stage 1 真正的資產。下面的缺口都比不上它們重要。

### 3.2 盲點（按影響排序）

**I-1 · `check_reading.py` 是回歸測試，不是活的偵測器。**
它讀的是 `var/raw/` 裡 20 份 **存檔 HTML**。若 Google Patents 明天改掉描述標記，
這支檢查 **仍然 20/20 全綠**，因為它比對的是舊檔案。`docs/05` §7 寫著
「The checker is the tripwire」——這句話**只在有人重新抓頁面時成立**。
今天沒有任何東西會重抓。這是本次回顧最重要的一條：**專案最活躍的外部依賴，
偵測器是靜態的。**
**已處理（2026-08-27）**：`tools/check_freshness.py` 從另一側補上——重抓三份取樣頁，
比對解析出來的結構數量。存檔仍然是基準線，探針不覆寫它。

**I-2 · schema 常數的規則只是一句叮嚀，沒有機制。**
`CLAUDE.md` 要求「改擷取器就在同一次 commit 動 `READING_SCHEMA` / `OPS_CARD_SCHEMA`」，
而這條規則是**踩過兩次**才寫下的。再讀（`service.py:555`、`:529`）時的重新推導機制是好的、
自動的；問題只在「忘記加一」——而防止忘記的目前手段是記憶。
這正是全域規則說的反例：規則應寫成**資產的性質**，不是對路徑的叮嚀。
**已處理（2026-08-27）**：`tests/test_extractor_fingerprint.py` 把擷取器的 AST（去掉
docstring）雜湊後釘在常數旁邊。實測過會叫：對 `_fig_number` 插入一行後即 FAIL，
還原後即 PASS。註解與排版不會誤觸。

**I-3 · 配額只被印出來，沒有被斷言。**
BR-6 的兩個實質承諾——「頁面位元組隨看隨取」「重讀不花配額」——都沒有檢查。
`Usage.summary()` 是給人看的字串。若某次重構讓建卡順手預抓縮圖，
所有 gate 仍然全綠，只有 EPO 的週計數器會安靜地爬。

**I-4 · 沒有一鍵執行，也沒有 CI。**
五支 gate 各自獨立執行，其中三支要網路與配額。實務上「守門」等於「我記得要跑」。
本機單人、無 remote，CI 不適用；但一支聚合器（先跑離線的，再問要不要跑耗配額的）
是成立的。
**已處理（2026-08-27）**：`tools/run_gates.py`，分三層 local／net／ops，
預設只跑不出網的四支；沒跑到的會被列成「not run」而不是消失。

**I-5 · 分數不可跨時間比較。**
`check_reading` 的「20/20」分母是 `var/raw/` 的檔案數，會隨取樣增加而變；
`check_search` 的「20/20」是檢查項數。兩個 20 意義不同，紀錄裡並排時看起來像同一種東西。
**已處理（2026-08-27）**：聚合器的 summary 每一列都印出該分數的單位
（per DOCUMENT／per CHECK／per METRIC）。

**I-6 · 兩支 gate 沒有校準案。**
`smoke_service.py` 與 `tests/test_ops_number_formats.py` 全綠，但沒有任何
「已知為真應被擋下」的反例證明它們有能力失敗。相較 §3.1 的紀律，這兩支是漏網的。

### 3.3 review-when 觸發器盤點（`docs/05` §7）

| 事實 | 若外界改變，誰會叫 |
|---|---|
| F-1 OPS 不供 US 全文 | **會叫**：`verify_ops.py` §4 的斷言反轉即 FAIL（刻意設計） |
| F-5/F-6/F-7 Google 標記語彙 | **會叫（2026-08-27 起）**：`check_freshness.py` 重抓三份取樣頁比對結構數量 |
| F-16 翻譯頁雙語 | 部分：`check_reading.py` 對 EP3000000A1 斷言 53 % 被丟棄——存檔頁；`check_freshness.py` 的取樣目前不含翻譯頁 |
| F-19 EN/DE/FR 附屬項 | **會叫**：`check_search.py` 九案雙向對照（純本地邏輯，不受外界影響） |
| F-3/F-4 OPS 影像格式與解析度 | **會叫**：`verify_ops.py` §3 實際向 OPS 取 |
| F-8 配額表頭 | 部分：讀得到就印，欄位若改名會靜默變空 |
| F-9/F-10 檢索上限與 `pn=` 行為 | **不會叫**：上限若改，UI 上「可達 2,000」會變成錯的字面數字 |
| F-13 Google 落後 OPS | **不會叫**。但這條相反：Google 追上只會讓卡片更好，不影響正確性——**不需要**觸發器 |
| F-14 號碼寬度依記錄而異 | **會叫**：`tests/test_ops_number_formats.py` |

---

## 4. 建議與落地結果（四條全數完成，2026-08-27）

> 前提：Stage 1 的核心需求已滿足，這四條沒有一條是「功能不足」。
> 它們買的是「壞掉時會被發現」。四支新東西**都不改任何既有行為**。

**建議 1 · 新鮮度探針（對應 I-1）── 已完成：`tools/check_freshness.py`**
重抓 `US6285999B1`（2001 年公告、限定事項為兄弟節點）、`US20250383260A1`
（2025 年公開、`ul.description` 版式）、`EP1000000A1`（EP 版式）三件——刻意一件一種
咬過人的標記版式——與存檔版比對 blocks／paras／claims／claim_parts／figrefs／chars，
容差 10 %，逐字內容不比。抓下來的頁面 **不寫回 `var/raw/`**，基準線因此不會被探針
自己汙染。首跑結果：三件全部**逐項完全相同**。校準：把活頁面的段落標記拿掉後
必須被抓到，實得 `blocks: 65 -> 7 (-89%)`。失敗語意分三級——0 通過、1 有偏移、
**2 無法判定**（抓不到就說抓不到，不冒充通過）。

**建議 2 · 擷取器指紋（對應 I-2）── 已完成：`tests/test_extractor_fingerprint.py`**
14 個閱讀擷取函式 ＋ 4 個 OPS 建卡函式，取 AST（去 docstring）雜湊釘在
`READING_SCHEMA` / `OPS_CARD_SCHEMA` 旁邊。用 AST 而不是原始碼字串，是為了讓註解與
排版改動不會誤觸——會誤觸的守門員很快就會被習慣性忽略。**實測過它會叫**：對
`_fig_number` 插入一行 → FAIL 並印出該用的新雜湊；`git checkout` 還原 → PASS。
另有一個單元校準（改註解不變、改回傳值必變）。基準線取自 `main@cacfde9`，
沒有為了建立基準而動任何常數。

**建議 3 · 版面自檢（對應 R-9／R-10）── 已完成：`tools/check_layout.py`**
以 DevTools 協定（`websockets` 已在環境內，不必裝 Playwright）驅動無頭 Chrome，
量的是**頁面自己的函式**（`measurePx()`、`READ`），不是把規則抄一份去測自己的抄本。
13 項全過，且複現了 §5.2 的紀錄數字：

| 檢查 | 結果 |
|---|---|
| R-9 無死區 | 1920：面板 726 px／欄 612 px，差 114 px；2560：面板 801 px／欄 684 px，差 117 px（上限 220） |
| R-9 無水平溢位 | 1920、2560 皆 `scrollWidth == clientWidth` |
| R-10 自動配對 | 1920 → 17px/72ch；2560 → 18px/76ch，都在 16–18px／68–76ch 範圍內 |
| R-10 反向 | 釘住 60ch 後跨兩個尺寸不變，且頁面自陳處於讀者模式 |
| 校準（兩個） | 面板改成 90 % → 報出 1620 px 死區（正是 R-9 要防的缺陷）；塞入 4000 px 元素 → 報出水平溢位 |

刻意不做的：像素級外觀、顏色、深色模式——那仍然是人眼的事，工具自己在結尾這樣寫。

**建議 4 · 一鍵聚合器（對應 I-4／I-5）── 已完成：`tools/run_gates.py`**
分三層：`local`（四支，完全不出網）、`--net`（加兩支抓 Google Patents 單件頁）、
`--all`（加兩支耗 OPS 配額，且執行前明白說出成本）。Summary 每列標明該分數的單位，
沒跑到的列成 `not run (net)` 並附一句「沒跑到的 gate 不等於通過的 gate」。
**全套 8 支實跑結果：全過**（見 §4.1）。

**維持不做**（理由不變）：BR-7 的 URL pin、R-7 的自動檢查、F-9/F-10 的上限監看、
I-6 的補校準。另外新增一條不做：`check_freshness` 的取樣**不擴到全部 20 件**——
每跑一次就是 20 次外部請求，而三件已足以偵測版式變更；真的要全面重掃時，
`tools/probe_coverage.py --refresh` 本來就在那裡。

### 4.1 落地後全套實跑（2026-08-27）

| Gate | 層 | 結果 | 耗時 |
|---|---|---|---|
| `tools/check_reading.py` | local | 20/20 文件 | 21.3 s |
| `tests/test_ops_number_formats.py` | local | 7/7 | 0.1 s |
| `tests/test_extractor_fingerprint.py` | local | 4/4 | 0.5 s |
| `tools/check_layout.py` | local | 13/13 | 6.0 s |
| `tools/smoke_service.py` | net | ALL PASS | 2.4 s |
| `tools/check_freshness.py` | net | 3 件逐項相同＋校準過 | 5.4 s |
| `tools/verify_ops.py` | ops | 14/14 | 13.0 s |
| `tools/check_search.py` | ops | 20/20 | 11.1 s |

OPS 配額：本次 `--all` 約 19 requests／0.8 MB，EPO 週計數器事後 13.2 MB（GMT 2026-W35）。

---

## 5. review-when（本文件自身）

- `var/raw/` 的取樣份數改變 → §2 的「20 份」與 §3.2 I-5 需重讀。
- 任何一支 `tools/check_*.py` 改動 → §1/§2 的「誰在守」欄位失效，需重跑比對。
- 建議 1–4 任一條落地 → 對應列的證據等級改變，回來改這張表，不要另開新檔。
  （2026-08-27：四條全數落地，§2／§3.2／§3.3／§4 已就地更新。）
- `check_layout.py` 的 `MAX_DEAD_STRIP=220` 是從 114/117 px 的實測加上餘裕訂的。
  段落編號側欄（`--gutter`, 3.6em）或 `.body` 內距若改，這個數字要重訂，
  不是把它調大到不會叫為止。
- 頁面若改掉 `measurePx()`／`READ`／`.pane.text`／`#tabbody .doc` 任何一個名字，
  `check_layout.py` 會以 `page threw` 直接失敗——那是正確行為，去改工具，不是繞過它。
- Stage 2 開工 → 本文件的「已滿足」前提僅涵蓋 Stage 1 範圍。
